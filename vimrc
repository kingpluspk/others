" vim simple config

syntax on			" 语法高亮
set nu              " 在左侧行号
set ts=4  			" (注：ts是tabstop的缩写，设TAB宽4个空格)
set cursorline      " 突出显示当前行
set ruler           " 在右下角显示光标位置的状态行
set autoindent      " 自动缩进
set expandtab
